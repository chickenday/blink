1：这是在master新建的文件，查看本机上执行git status是否会显示远程仓库有更新。
2：这次是检测两个人clone同一个远程分支，各修改其中一个文件，但是如何模仿两个呢。可以用一个用户，分别见两个分支模仿，只要先后提交，反正合并是在都提交后，在github上合并。可以新新建两个分支追踪同一个远程分支。
3：这是newbranch1的修改，修改文件1，push。然后，newbranch2修改文件2，push。由于两个分支名，远程都没有，所以push时会新建。然后到github上合并这两个分支，看是否会自动合并。不过有点没道理，因为这两个新分支新建时，会知道是基于origin/master吗，先看是否能自动合并，如果能，说明知道。